#include <stdio.h>
#include <stdlib.h>

int main()
{
  char name[] = "Alif";
  int age = 20;
  float gpa = 3.5;
  char avg_grade = 'A';

  printf("Hello %s welcome ot the program\n", name);
  printf("You are %d years old.\n", age);
  printf("And your gpa is %f", gpa);
  printf("And your average age is %c", avg_grade);

  return 0;
}